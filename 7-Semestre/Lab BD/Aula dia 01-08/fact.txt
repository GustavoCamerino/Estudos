USE lab1;
DELIMITER //

CREATE FUNCTION fact(num INT) returns int reads sql data
BEGIN
    DECLARE factorial INT DEFAULT 1;
    DECLARE i INT DEFAULT 1;

    WHILE i <= num DO
        SET factorial = factorial * i;
        SET i = i + 1;
    END WHILE;

    RETURN factorial;
END //

DELIMITER ;
